const NoUserCard = () => {
    return (
        <div className="min-h-screen flex items-center justify-center bg-black">
            <div className="min-h-screen flex items-center justify-center bg-black ">
                <h1 className="text-5xl font-bold tracking-wide text-gray-200 animate-pulse">
                    Loading . . .
                </h1>
            </div>

        </div>
    );
};

export default NoUserCard;
