import {configureStore} from "@reduxjs/toolkit"

export const store = configureStore({
    reducer:{
        // here we will create slices -> like features for different things like for comments for follow for like 
    },
});