![alt text](image.png)  -- Real dom 
-- here is real DOM if u want to change even a h1 content then whole DOM refresh and it takes time and network
-- now in case of virtual , its just a copy of Real DOM and whatever changes u need to make virtual DOM focuses on that particular tag or content only , NO Uncessary Reloading and rerendering 

-- jSX is a file type with react icon in which we can write both HTML and JS at same time for eg 
we Defined a fucntion called Hero() then that fuction can be call boht like 1. Hero() and 2. <Hero />